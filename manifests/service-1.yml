apiVersion: v1
kind: Service
metadata:
    name: "demov2-0c15"
    labels:
        app: "demov2-0c15"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "demov2-0c15"